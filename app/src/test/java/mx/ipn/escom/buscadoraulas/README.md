# Tests

This directory contains unit and route-validation tests used during development. Some tests print or generate diagnostic route output; generated text files and build artifacts are not required to run the application.
