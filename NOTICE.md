## Repository contents

This public repository contains the Android application source code and project configuration required for the BuscadorAulas ETS project. Local IDE state, build outputs, SDK paths, APKs and device/debug logs are intentionally excluded through `.gitignore`.
